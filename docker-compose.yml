version: "3.8"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - ./server/.env
    depends_on:
      - mongodb

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
